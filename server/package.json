{
  "name": "blog-app-server",
  "version": "1.0.0",
  "description": "REST API backend a Blog App-hez (Express + SQLite)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node src/seed.js",
    "test": "mocha --timeout 10000 test/**/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "chai": "^4.4.1",
    "mocha": "^10.7.3",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
